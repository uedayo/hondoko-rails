- flash.each do |name, msg|
  - if msg.is_a?(String)
    .alert.alert-top{class: "#{bootstrap_class_for(name)}"}
      %a.close{"data-dismiss" => "alert"} ×
      = content_tag :div, msg, :id => "flash_#{name}"
