.inside
  %p= t('view.item_count_message', count: @count)
= render 'books/book'
= simple_form_for @item, html: {class: 'form-horizontal'} do |f|
  = f.hidden_field :book_id, class: 'form-control input-lg width-auto'
  - if @item.errors.any?
    .alert.alert-danger
      %ul
        - @item.errors.full_messages.each do |msg|
          %li= msg
  .form-group
    = f.label :area, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.collection_select :area_id, Area.all, :id, :name, {}, {class: 'form-control'}
  .form-group
    .center-horizontal= f.submit class: 'btn btn-primary'
